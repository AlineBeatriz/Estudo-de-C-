using System;

namespace Aula02 //namespace-(espaço de nome) Os namespace são usados para organizar classes. Para usar uma classe de outro namespace, colocamos -using(usando)- no começo do código.
{
    class program
    {
        static void Main(string[] args)//string[] args (vai receber os Argumento de Entrada, um arrya)
        {
            Console.WriteLine("Olá, mundo!!");//writeLine(escrever linha), serve para quebrar a linha no final do texto.
        }
    }
}