using System;

class aula07{
    static void Main(){
        const string canal = "CFB cursos"; //(string-corda) é uma coleção sequencial de caracteres que é usada para representar texto.
        const double pi = 3.1415; //(double-dupla) usada para declarar uma variável dupla.

        Console.WriteLine("canal; {0} \n pi; {1}", canal, pi);
    }
}